// Code generated by goa v3.25.3, DO NOT EDIT.
//
// lfx-v2-campaign-service-reconciliation service
//
// Command:
// $ goa gen github.com/linuxfoundation/lfx-v2-campaign-service/design

package lfxv2campaignservicereconciliation

import (
	"context"

	"goa.design/goa/v3/security"
)

// Inspect and resolve dispatch states the service deliberately leaves for a
// human.
type Service interface {
	// List everything in this project needing operator attention: stranded
	// dispatch claims, campaigns whose upstream create is UNCONFIRMED, and
	// partially-built audiences. Read-only and always safe.
	GetReconciliation(context.Context, *GetReconciliationPayload) (res *ReconciliationReport, err error)
	// Release a STRANDED dispatch claim so the (brief, platform) pair can be
	// dispatched again.

	// Deliberately narrow. It refuses unless the claim carries NO evidence of an
	// upstream create (no platform_campaign_id and no result blob), is still
	// 'pending', and is older than the release floor — a claim younger than that
	// may still be in flight, and releasing it would let a concurrent dispatch
	// double-create a paid campaign.

	// Requires If-Match carrying the version the operator observed in the report.
	// That is not ceremony: a claim can be legitimately re-claimed by a new
	// dispatch between the report and this call, which bumps the version. Without
	// the gate that live claim would be deleted. Returns 409 when the item is not
	// releasable and 412 when it changed underneath the operator.
	ReleaseDispatchClaim(context.Context, *ReleaseDispatchClaimPayload) (res *ReconciliationItem, err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// JWTAuth implements the authorization logic for the JWT security scheme.
	JWTAuth(ctx context.Context, token string, schema *security.JWTScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "lfx-v2-campaign-service"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "lfx-v2-campaign-service-reconciliation"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [2]string{"get-reconciliation", "release-dispatch-claim"}

// GetReconciliationPayload is the payload type of the
// lfx-v2-campaign-service-reconciliation service get-reconciliation method.
type GetReconciliationPayload struct {
	// JWT token issued by Heimdall
	BearerToken *string
	// Project UUID or slug that scopes the connection
	ProjectID string
}

// ReconciliationItem is the result type of the
// lfx-v2-campaign-service-reconciliation service release-dispatch-claim method.
type ReconciliationItem struct {
	// What kind of stuck state this is
	Kind string
	// Owning brief
	BriefID string
	// Platform the stuck state belongs to (empty for an audience item)
	Platform *string
	// Campaign row UUID (stuck_claim / unconfirmed_campaign)
	CampaignID *string
	// Audience row UUID (partial_audience)
	AudienceID *string
	// The row's current persisted status
	Status string
	// Upstream campaign id, when one was recorded. Its PRESENCE is what makes an
	// item non-releasable: it proves something exists upstream.
	PlatformCampaignID *string
	// How long the row has been in this state
	AgeSeconds int64
	// Optimistic-concurrency version; supply as If-Match to resolve
	Version *int64
	// ETag to supply as If-Match when resolving
	Etag *string
	// Whether release-claim can act on this item. False = resolve it out of band;
	// the service will refuse.
	Resolvable bool
	// Why this item is stuck and what the operator must verify upstream before
	// acting
	Detail string
}

// ReconciliationReport is the result type of the
// lfx-v2-campaign-service-reconciliation service get-reconciliation method.
type ReconciliationReport struct {
	// Project this report covers
	ProjectID string
	// Items needing attention, oldest first
	Items []*ReconciliationItem
	// Total items found (may exceed len(items) when truncated)
	Total int64
	// True when more items exist than were returned
	Truncated bool
}

// ReleaseDispatchClaimPayload is the payload type of the
// lfx-v2-campaign-service-reconciliation service release-dispatch-claim method.
type ReleaseDispatchClaimPayload struct {
	// JWT token issued by Heimdall
	BearerToken *string
	// Project UUID or slug that scopes the connection
	ProjectID string
	// Brief UUID
	BriefID string
	// Campaign UUID
	CampaignID string
	// If-Match header carrying the current ETag/version
	IfMatch *string
	// Operator asserts they checked the ad platform and NO campaign exists for
	// this brief/platform. Must be true; the service cannot verify it and will not
	// guess.
	VerifiedAbsent bool
}

type BadRequestError struct {
	// HTTP status code
	Code string
	// Error message
	Message string
}

type ConflictError struct {
	// HTTP status code
	Code string
	// Error message
	Message string
}

type ConnServiceUnavailableError struct {
	// HTTP status code
	Code string
	// Error message
	Message string
}

type InternalServerError struct {
	// HTTP status code
	Code string
	// Error message
	Message string
}

type NotFoundError struct {
	// HTTP status code
	Code string
	// Error message
	Message string
}

type PreconditionFailedError struct {
	// HTTP status code
	Code string
	// Error message
	Message string
}

type PreconditionRequiredError struct {
	// HTTP status code
	Code string
	// Error message
	Message string
}

// Error returns an error description.
func (e *BadRequestError) Error() string {
	return ""
}

// ErrorName returns "bad-request-error".
//
// Deprecated: Use GoaErrorName - https://github.com/goadesign/goa/issues/3105
func (e *BadRequestError) ErrorName() string {
	return e.GoaErrorName()
}

// GoaErrorName returns "bad-request-error".
func (e *BadRequestError) GoaErrorName() string {
	return "BadRequest"
}

// Error returns an error description.
func (e *ConflictError) Error() string {
	return ""
}

// ErrorName returns "conflict-error".
//
// Deprecated: Use GoaErrorName - https://github.com/goadesign/goa/issues/3105
func (e *ConflictError) ErrorName() string {
	return e.GoaErrorName()
}

// GoaErrorName returns "conflict-error".
func (e *ConflictError) GoaErrorName() string {
	return "Conflict"
}

// Error returns an error description.
func (e *ConnServiceUnavailableError) Error() string {
	return ""
}

// ErrorName returns "conn-service-unavailable-error".
//
// Deprecated: Use GoaErrorName - https://github.com/goadesign/goa/issues/3105
func (e *ConnServiceUnavailableError) ErrorName() string {
	return e.GoaErrorName()
}

// GoaErrorName returns "conn-service-unavailable-error".
func (e *ConnServiceUnavailableError) GoaErrorName() string {
	return "ServiceUnavailable"
}

// Error returns an error description.
func (e *InternalServerError) Error() string {
	return ""
}

// ErrorName returns "internal-server-error".
//
// Deprecated: Use GoaErrorName - https://github.com/goadesign/goa/issues/3105
func (e *InternalServerError) ErrorName() string {
	return e.GoaErrorName()
}

// GoaErrorName returns "internal-server-error".
func (e *InternalServerError) GoaErrorName() string {
	return "InternalServerError"
}

// Error returns an error description.
func (e *NotFoundError) Error() string {
	return ""
}

// ErrorName returns "not-found-error".
//
// Deprecated: Use GoaErrorName - https://github.com/goadesign/goa/issues/3105
func (e *NotFoundError) ErrorName() string {
	return e.GoaErrorName()
}

// GoaErrorName returns "not-found-error".
func (e *NotFoundError) GoaErrorName() string {
	return "NotFound"
}

// Error returns an error description.
func (e *PreconditionFailedError) Error() string {
	return ""
}

// ErrorName returns "precondition-failed-error".
//
// Deprecated: Use GoaErrorName - https://github.com/goadesign/goa/issues/3105
func (e *PreconditionFailedError) ErrorName() string {
	return e.GoaErrorName()
}

// GoaErrorName returns "precondition-failed-error".
func (e *PreconditionFailedError) GoaErrorName() string {
	return "PreconditionFailed"
}

// Error returns an error description.
func (e *PreconditionRequiredError) Error() string {
	return ""
}

// ErrorName returns "precondition-required-error".
//
// Deprecated: Use GoaErrorName - https://github.com/goadesign/goa/issues/3105
func (e *PreconditionRequiredError) ErrorName() string {
	return e.GoaErrorName()
}

// GoaErrorName returns "precondition-required-error".
func (e *PreconditionRequiredError) GoaErrorName() string {
	return "PreconditionRequired"
}
