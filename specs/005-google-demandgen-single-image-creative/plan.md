# Implementation Plan: Google Demand Gen single-image ad creative (uploadable)

**Branch**: `feat/google-demandgen-creatives` (stacked on `feat/meta-image-video-creatives`) | **Date**: 2026-08-17 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/005-google-demandgen-single-image-creative/spec.md`

## Summary

Turn Google's Demand Gen path from a paused shell into a launch-ready image ad. A caller
references an uploaded image asset (via the existing platform-agnostic
`POST .../briefs/{brief_id}/creative-assets` endpoint) from a Google Demand Gen
campaign-create request. At dispatch the Google client uploads the bytes to the resolved
customer as an `ImageAsset` (`assets:mutate`), then `CreateDemandGenCampaign` — which today
stops at the ad group — creates a real **Demand Gen ad** referencing that image (plus the
business name, logo, and UTM-tagged final URL). The ad construction sits behind a
format-keyed builder so carousel and video can follow without touching the image path. The
status-toggle ACTIVATE gate is made channel-aware so a keyword-less-but-provisioned Demand
Gen campaign is activatable. Activation stays explicit; nothing auto-activates.

This slice is deliberately smaller than the Meta slice because storage is reused: **no new
table, migration, upload endpoint, repo, or service handler.** The work is confined to the
Google client, the Google dispatcher, the status-toggle gate, and docs.

## Technical Context

**Language/Version**: Go (per `go.mod` — authoritative)

**Primary Dependencies**: Goa v3.25.3 (HTTP contract codegen), pgx/v5 (Postgres, reused
`CreativeAssetRepo`), existing Google Ads REST client (`internal/platform/googleads`).

**Storage**: PostgreSQL — the `creative_assets` `bytea` table from the Meta slice, reused
unchanged. No new DDL.

**Testing**: `go test` (unit, no DB) for the ad/creative builder, asset-upload idempotency,
final-URL/validation, and the channel-aware activation gate; the existing httptest-server
pattern in `internal/platform/googleads/*_test.go` for `assets:mutate` and
`adGroupAds:mutate`; the in-package live-DB test convention (gated on `TEST_DATABASE_URL`)
for any dispatch path that loads asset bytes.

**Target Platform**: Linux server (Kubernetes, Helm chart under `charts/`)

**Project Type**: Web service (Go/Goa HTTP API); front-end (`lfx-self-serve`) is a separate
repo, out of scope.

**Constraints**: Backward compatible — the no-image Demand Gen path MUST remain today's
paused shell. All ad input validated before the first paid mutate (FR-008). `assets:mutate`
idempotent per (customer + checksum). A non-idempotent `:mutate` create is never retried
(existing GA convention). Generated code only via `design/` + `make apigen`; no edits to
`gen/**`.

**Scale/Scope**: Edits to `internal/platform/googleads` (new asset-upload + Demand Gen ad
builder), `internal/dispatch/googleads.go` (asset resolution + config), the status-toggle
gate, plus OKF docs. Single-image only. Likely **no DSL change** — the Google `config`
envelope is `Any`, so the creative reference is a struct/doc change, not a Goa type change
(confirmed in G1).

## Constitution Check

*GATE: re-checked after design below.*

- **Generated contract, not hand-written** — if any HTTP contract surface changes it is made
  in `design/` and regenerated with `make apigen`; no edits to `gen/**`. The Google `config`
  envelope is `Any`, so the creative reference is expected to need no DSL change. ✅
- **Credentials are secrets at rest** — image bytes are not credentials; the `assets:mutate`
  upload uses the already-decrypted OAuth access token via the existing client auth path; no
  credential is logged/returned/persisted in plaintext. ✅
- **Project-nested, gateway-authz** — no new endpoint; the reused upload endpoint is already
  project-nested and the create path is unchanged in its routing. Authz stays at the gateway. ✅
- **DB-less mode supported** — the asset load reuses the existing repo, which degrades exactly
  as current persistence does when the DB is absent; no new always-on DB dependency for
  liveness. ✅

No constitution violations; Complexity Tracking not required.

## Project Structure

### Documentation (this feature)

```text
specs/005-google-demandgen-single-image-creative/
├── spec.md              # Done
├── plan.md              # This file
├── research.md          # Demand Gen ad type/fields, image constraints, assets:mutate dedupe (Phase 0)
├── data-model.md        # CampaignInput/Result creative-field additions (Phase 1) — no new table
├── contracts/           # googleAdsConfig creative-reference shape (Phase 1)
└── tasks.md             # /speckit-tasks output (later)
```

### Source Code (repository root)

```text
gen/**                           # regenerated by `make apigen` IF any DSL changes — never hand-edited

internal/
├── dispatch/
│   └── googleads.go             # extend googleAdsConfig with mediaFormat + imageAssetId(s) + businessName + logo;
│                                #   resolve asset bytes (resolveVariantAssets analogue); channel-aware ACTIVATE gate
└── platform/googleads/
    ├── assets.go                # NEW: uploadImageAsset() — assets:mutate ImageAsset, content-addressed/idempotent
    ├── demandgen.go             # extend CreateDemandGenCampaign: create a Demand Gen ad when a creative ref is present
    └── demandgen_ad.go          # NEW: demandGenAdBuilder — format-keyed (single image today; carousel/video later)

docs/knowledge/code/internal-platform-googleads.md   # concept update (Demand Gen no longer shell-only)
docs/knowledge/code/index.md                          # verbatim bullet update
docs/api-catalog.md                                   # Google section update (creative reference fields)
docs/knowledge/log/2026-08-17-<ticket>-google-demandgen-single-image.md   # NEW log entry
```

**Structure Decision**: Follows the repo's existing layering exactly — no new top-level
structure, and (unlike the Meta slice) no new `design/` method, migration, repo, or service
handler, because the upload contract + storage are reused. The change is concentrated in the
Google platform client and its dispatcher.

## Implementation approach (ordered, each an independently reviewable commit)

### G1 — Contract + config: creative reference on the Google path (`internal/dispatch/googleads.go`, `internal/platform/googleads/campaign.go`; `design/` only if needed)
- Extend `googleAdsConfig` (the wire shape mapped from the `Any` config envelope) with an
  optional creative block: `mediaFormat` (enum, `single_image` today), `imageAssetIds`
  (`asset_id` UUIDs), `businessName`, and `logoAssetId`.
- Extend `CampaignInput` with the matching optional fields the client consumes.
- Confirm no Goa DSL change is required (the `config` envelope is `Any`); if a documented
  example or catalog type must change, do it in `design/` + `make apigen` and commit `gen/**`
  together. Otherwise this is a struct/doc-only commit.
- Backward compat: absent creative block → `CampaignInput` carries no creative → today's
  shell behaviour is unchanged.

### G2 — Google client: image `Asset` upload (`internal/platform/googleads/assets.go`)
- `uploadImageAsset(ctx, bytes, checksum) (assetResourceName string, err error)` →
  `assets:mutate` with an `ImageAsset { data: <base64> }`. Reuses the client's request layer
  (`doRequest`, non-idempotent `:mutate` — NOT retried on 429), error vocabulary
  (`apiError`/`transportError`/pre-send), and `firstResourceName` extraction.
- Idempotent per (customer + checksum): a per-dispatch cache keyed by (customerID, checksum);
  the resolved resource name is persisted in `CampaignResult` (G4/G5) so a reconcile/retry
  reuses it. Google-side dedupe of identical image bytes is confirmed in `research.md`; the
  cache is the guaranteed layer regardless.
- Content-addressed error handling mirrors the Meta `uploadImage` shape (and applies the
  same token-path discipline noted in the Meta token-trim audit — the access token is used
  exactly as the create/discovery paths resolve it, never a divergent copy).

### G3 — Google client: Demand Gen ad builder (`internal/platform/googleads/demandgen_ad.go`, `demandgen.go`)
- `demandGenAdBuilder` keyed by media format. `singleImageDemandGenAd` produces the v23
  Demand Gen responsive single-image ad payload (marketing image asset, business name, logo
  asset, headlines/descriptions via the existing `composeAdCopy`, final URL via
  `buildAdFinalURL`). Carousel/video builders are the planned future implementations of the
  same interface (FR-006 / SC-005) — not built now. Exact v23 ad type + required-field set is
  pinned in `research.md`.
- Extend `CreateDemandGenCampaign`: after the ad group commits, when a creative reference is
  present, upload the image asset (G2), build the ad, and `adGroupAds:mutate` it `PAUSED`.
  When absent, keep today's shell close-out step verbatim (backward compatible).
- **Stamp the campaign channel on the result.** `CampaignResult` gains an explicit
  `Channel` field (values `search` / `demand_gen`), stamped at create by BOTH paths —
  `CreateDemandGenCampaign` writes `demand_gen`, the Search `CreateCampaign` writes `search`.
  This mirrors the existing `CustomerID` field, which is stamped on every result (including
  partials) precisely so a later toggle can make a decision the id alone can't support. The
  activation gate (G5) reads this field rather than inferring the channel from the presence of
  keyword criteria vs. asset resource names — inference is fragile (a Search campaign
  legitimately has zero keywords mid-provisioning; a future Demand Gen format may carry
  neither today's shapes) and an activation decision on a paid campaign must not rest on it.
  It is stamped on partials too, so a reconcile/toggle of an ambiguously-created campaign
  still knows its channel. A result predating the field (empty `Channel`) falls back to the
  current keyword-criterion check, exactly as the `CustomerID` account-mismatch guard skips
  when the id is absent on an older row.
- Validate ALL ad input (business name, logo resolvable, ≥1 marketing image, final URL) in a
  `precompute`-style step BEFORE the budget mutate (FR-008), mirroring the Search path's
  `precomputeAdGroupAdInputs`.
- Partial-result contract (FR-007): the asset and ad steps each extend the existing
  `namePartial`/`budgetPartial`/`campaignPartial` chain so an ambiguous failure after the ad
  group returns a non-nil result carrying the asset resource name + any ad id.

### G4 — Dispatch wiring (`internal/dispatch/googleads.go`)
- Map the G1 creative config into `CampaignInput`.
- Resolve each `imageAssetId`/`logoAssetId` to bytes via the reused `CreativeAssetRepo`,
  scoped to (project, brief) — the `resolveVariantAssets` analogue from the Meta dispatcher.
  A malformed/absent/cross-brief asset fails the dispatch through the existing `notCreated`
  path, releasing the claim rather than stranding it (FR: no silent shell fallback).
- Persist the resolved image `Asset` resource name(s) + ad id in the campaign `result` JSONB.

### G5 — Channel-aware ACTIVATE gate (`internal/dispatch/googleads.go`)
- Today `ToggleStatus` refuses ACTIVATE unless GA-4 persisted ≥1 **keyword** criterion — a
  Search-only condition that would wrongly refuse a fully-provisioned Demand Gen campaign.
- Make the gate channel-aware, keyed on the explicit `CampaignResult.Channel` field stamped at
  create (G3) — NOT inferred from keyword/asset presence:
  - `Channel == "demand_gen"` → "provisioned" = a Demand Gen ad with its required assets exists
    (recovered from the `result` blob, like `googleAdsChildIDs`).
  - `Channel == "search"` → the existing ≥1-keyword-criterion condition, unchanged.
  - empty `Channel` (result predates the field) → fall back to the current keyword check, so no
    older Search campaign's gate behaviour changes.
- An unprovisioned campaign of either channel is still refused with `ErrCampaignNotProvisioned`
  (409, Google never contacted). ACTIVATE still cascades children-first.

### G6 — Tests
- Unit (no DB): Demand Gen ad-builder output for single-image vs no-creative; `uploadImageAsset`
  idempotency via httptest server asserting exactly one `assets:mutate` per (customer,
  checksum); pre-mutate validation matrix (missing business name/logo/image, bad final URL,
  under-dimension image); channel-aware activation gate (Demand-Gen-provisioned activatable,
  unprovisioned refused, Search unchanged); partial-result arms on each ambiguous failure.
- Integration (PG16 dbtest / in-package live convention): dispatch loads asset bytes from the
  reused repo and produces a Demand Gen ad against a stubbed Google Ads server; missing asset
  fails the dispatch cleanly.

### G7 — OKF knowledge base (required for CI `validate-okf`)
- Update `docs/knowledge/code/internal-platform-googleads.md` (Demand Gen is no longer a
  shell; document `uploadImageAsset`, the `demandGenAdBuilder`, and the channel-aware
  activation gate) and its `docs/knowledge/code/index.md` bullet **verbatim**.
- Update the Google section of `docs/api-catalog.md` (creative-reference config fields).
- Add `docs/knowledge/log/2026-08-17-<ticket>-google-demandgen-single-image.md`
  (`**Creation**` marker).
- `go run ./cmd/okfvalidate ./docs/knowledge` MUST pass. Do **not** run `okfgen`.

## Post-implementation gates (per CLAUDE.md)
- After each signed commit: `/lfx-skills:lfx-local-review`; land fixes as `fix(<scope>): …`
  signed commits; rerun the full trio. Cycle stops at PR-open.
- Before PR: `make all` (clean apigen fmt lint test build) + `okfvalidate`. CI runs
  build+test (PG16), mega-linter, license-header-check, validate-okf, ko-build.

## Risks / open items
- **Demand Gen ad type + required fields** — the exact v23 ad resource
  (`DemandGen*AdInfo`), the required business name / logo / marketing-image counts, and
  min-dimension / aspect constraints must be pinned in `research.md` and, where feasible,
  validated with a `validateOnly` `adGroupAds:mutate` against a real account BEFORE coding G3
  (the same way the Demand Gen `targetSpend` bidding strategy was live-verified in
  LFXV2-3257). The ad-create commits after four paid mutates, so a wrong payload orphans them.
- **`assets:mutate` dedupe** — confirm whether Google returns the existing asset for
  identical bytes or errors; the per-dispatch cache is the guaranteed idempotency layer
  regardless.
- **Stacked branch** — this depends on `creative_assets` from the Meta branch; rebase onto
  main once that merges. If Misha wants Google independent of Meta, the storage commits would
  need cherry-picking (larger, avoided by stacking).
- **Business name / logo source** — whether these come from the brief, the connection, or the
  config envelope is a data-model question resolved in `data-model.md`.
- **Ticket** — file LFXV2-XXXX (under umbrella LFXV2-2665) and rename the log entry slug.
